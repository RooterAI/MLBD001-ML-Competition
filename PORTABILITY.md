# 🔄 Портативность проекта

## ✅ Проект полностью автономен!

Этот проект разработан с учётом полной портативности и может быть запущен на любой машине с Python 3.8+ без привязки к конкретным путям.

## 🎯 Ключевые особенности

### 1. Относительные пути
Все модули используют относительные пути, вычисляемые динамически:
```python
import os
project_root = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
data_path = os.path.join(project_root, 'data', 'cleaned', 'cleaned_data.csv')
```

### 2. Автоматическая настройка окружения
Скрипты запуска автоматически:
- Определяют корневую директорию проекта
- Создают виртуальное окружение (если отсутствует)
- Устанавливают все зависимости
- Устанавливают правильную рабочую директорию

### 3. Кросс-платформенная совместимость
Поддерживаются все основные платформы:
- **Windows**: `start.bat` или `start.ps1`
- **Linux/Mac**: `start.sh`

## 📦 Перенос проекта

### Шаг 1: Скопируйте папку проекта
```bash
# Вся папка MLBD001 может быть скопирована куда угодно
cp -r MLBD001 /new/location/
```

### Шаг 2: Запустите на новой машине
```bash
cd /new/location/MLBD001
./start.sh  # Linux/Mac
# или
start.bat   # Windows
```

Вот и всё! Никаких дополнительных настроек не требуется.

## 🔍 Проверка портативности

Проект был протестирован на:
- ✅ Windows 10/11
- ✅ Linux (Ubuntu, Debian)
- ✅ macOS
- ✅ Различных Python версиях (3.8, 3.9, 3.10, 3.11, 3.12)

## 🛠️ Технические детали

### Структура путей
```python
# Все модули используют этот паттерн:
project_root = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

# Примеры:
data_path = os.path.join(project_root, 'data', 'cleaned', 'cleaned_data.csv')
model_path = os.path.join(project_root, 'models', 'model.pkl')
reports_path = os.path.join(project_root, 'reports', 'eda_plots.png')
```

### Смена рабочей директории
Веб-приложения (app.py, api.py) автоматически меняют рабочую директорию:
```python
project_root = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
os.chdir(project_root)  # Устанавливаем корень проекта как рабочую директорию
```

## ⚡ Быстрый тест

Проверьте портативность локально:

```bash
# 1. Скопируйте проект в другую директорию
cp -r MLBD001 /tmp/test_mlbd001

# 2. Запустите из новой локации
cd /tmp/test_mlbd001
./start.sh

# 3. Проверьте работу веб-приложения
# Откройте http://localhost:8501 в браузере
```

## 📝 Примечания

- **Не требуется** изменение конфигурационных файлов
- **Не требуется** установка глобальных зависимостей
- **Не требуется** настройка переменных окружения
- **Не требуется** администраторских прав (кроме установки Python)

## 🚀 Деплой на сервер

Для развёртывания на сервере:

```bash
# 1. Скопируйте проект
scp -r MLBD001 user@server:/path/to/deploy/

# 2. Запустите на сервере
ssh user@server
cd /path/to/deploy/MLBD001
./start.sh
```

Проект готов к работе в любой среде! 🎉
