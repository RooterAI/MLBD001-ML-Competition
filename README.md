# ML Проект - Конкурсное задание МОиБД

## 📝 Описание проекта
Максимально упрощенная, но работоспособная реализация конкурсного задания по машинному обучению.

**🔄 Проект полностью автономен и портативен!** Работает на любой машине с Python 3.8+ без привязки к абсолютным путям.

## Структура проекта
```
.
├── data/                    # Данные
│   ├── raw/                # Исходные данные
│   └── cleaned/            # Обработанные данные
├── src/                    # Исходный код
│   ├── module_a.py         # Предобработка данных
│   ├── module_b.py         # Разведочный анализ
│   ├── module_c.py         # Обучение модели
│   └── app.py              # Веб-приложение
├── models/                 # Сохраненные модели
├── reports/                # Отчеты и результаты
├── requirements.txt        # Зависимости
└── README.md              # Документация
```

## Установка
```bash
pip install -r requirements.txt
```

## Запуск

### 🚀 Автоматический запуск (рекомендуется)

Проект полностью автономен и работает на любой машине с Python 3.8+

**Windows (PowerShell):**
```powershell
.\start.ps1
```

**Windows (CMD):**
```cmd
start.bat
```

**Linux/Mac:**
```bash
chmod +x start.sh
./start.sh
```

Скрипты автоматически:
- Создают виртуальное окружение (если нет)
- Устанавливают все зависимости
- Запускают все модули обработки данных
- Открывают веб-приложение

### 📝 Ручной запуск (пошаговый)

### Модуль A: Предобработка данных
```bash
python src/module_a.py
```

### Модуль B: Разведочный анализ
```bash
python src/module_b.py
```

### Модуль C: Обучение модели
```bash
python src/module_c.py
```

### Модуль D: Веб-приложение
```bash
streamlit run src/app.py
```

## Результаты
- **Модуль A**: Очищенные данные в `data/cleaned/cleaned_data.csv`
- **Модуль B**: Графики в `reports/eda_plots.png` и выводы в `reports/eda_conclusions.txt`
- **Модуль C**: Модель в `models/model.pkl` с accuracy ~85%
- **Модуль D**: Работающее веб-приложение для предсказаний

## Архитектура решения
Используется простой подход:
1. **Данные**: CSV файл с базовой предобработкой
2. **Модель**: RandomForestClassifier с параметрами по умолчанию
3. **Интерфейс**: Streamlit для простого веб-интерфейса
4. **Метрики**: Accuracy как основная метрика

## 🔄 Портативность
Проект использует только относительные пути и работает из любой директории.
Все пути вычисляются динамически относительно корня проекта.

Подробнее см. [PORTABILITY.md](PORTABILITY.md)

## Автор
Участник конкурса МОиБД